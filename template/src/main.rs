// use std::

const INPUT: &str = include_str!("../input.txt");

fn main() -> Result<(), failure::Error> {
    // solve problem
    Ok(())
}

#[cfg(test)]
mod test {
    use super::*;

    #[test]
    fn test_something() {
        assert_eq!(1, 1)
    }
}
